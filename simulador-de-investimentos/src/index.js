import React from 'react';
import ReactDOM from 'react-dom';
import Simulador from "./componentes/app.jsx"

ReactDOM.render(
  <React.StrictMode>
    <Simulador />
  </React.StrictMode>,
  document.getElementById('root')
);
